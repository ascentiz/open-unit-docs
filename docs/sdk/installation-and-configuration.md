# Installation and Configuration

Status: To be completed. The actual wheel, package name, supported Python versions, operating systems, CPU architectures, and dependencies have not yet been provided.

## Installation template

Use an isolated Python environment compatible with the published wheel. Replace the illustrative path below with the actual release file; these commands do not confirm release compatibility.

```bash
python -m pip install "/path/to/your-sdk.whl"
python -m pip check
```

Document the download source, release version, compatibility requirements, dependencies, configuration fields, and installation verification. Installing the SDK is not an operating-system installation or firmware update.

Continue with [Basic Usage](basic-usage.md).
