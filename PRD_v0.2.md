# BodyOS / Open Unit 文档站 PRD

版本：v0.2 · 中英双语改版  
日期：2026-09-16  
现状基线：[PRD_v0.1.md](PRD_v0.1.md)

## 本轮目标

为现有文档站提供英文与简体中文两套完整内容，降低中文开发者的阅读门槛，同时保留现有英文网址及平台安全边界。

## 功能范围

- 英文为默认语言，沿用原有根路径；中文位于 `/zh/`。
- 页头提供 English / 简体中文切换，切换到另一语言的对应页面。
- 为现有全部 22 页提供中文正文、标题、表格及视频说明。
- 中文导航、目录、搜索提示及复制提示随语言切换。
- 搜索支持中文与英文内容。
- 中英文共用品牌图片及 CSS，代码、接口字段、单位和状态名保持一致。
- 保留接口占位性质、版本仍在演进及所有安全注意事项。
- 移除原入门页中指向缺失文件 `Connect B-core pi 1.md` 的无效链接；本轮不新增硬件连接教程。

## 技术方案

- 继续使用 MkDocs、Material for MkDocs 及 GitHub Pages。
- 增加 `mkdocs-static-i18n` 插件，采用文件后缀结构。
- 英文使用 `page.md`，中文使用同目录下的 `page.zh.md`。
- 导航沿用原路径，通过 `nav_translations` 提供中文名称。
- 配置 `site_url`，确保 GitHub Pages 子路径下语言链接和规范网址正确。
- 添加 `scripts/check_i18n.py`，检查翻译覆盖、代码一致性、生成页面、语言链接、资源及搜索索引。

## 验收标准

- `mkdocs build --strict` 成功。
- `python scripts/check_i18n.py` 成功。
- 英文和中文各生成 22 个内容页面。
- 桌面和手机均可切换语言，深层页面切换后仍处于对应章节。
- 中文搜索可检索中文关键词，英文搜索仍可使用。
- 中文页面无资源丢失或内部链接跳出中文路径的问题。
- 中英文示例代码保持一致，安全约束翻译不改变原意。

## 不在本轮范围

- 统一 BodyOS 与 Open Unit 品牌名称。
- 实现 SDK、设备控制、登录或后台 CMS。
- 替换测试视频、增加第三种语言或文档版本管理。
- 未经用户后续指示，不推送 GitHub 或发布线上改版。

## 本地验证记录

- 严格构建通过：MkDocs 1.6.1、Material 9.7.7、Static i18n 1.3.1，Python 3.11。
- 双语一致性检查通过：22 页英文与 22 页中文，代码、语言标记、对应语言链接、编辑链接、内部资源、锚点及搜索索引。
- Playwright 桌面验证：中文“遥测”返回相关结果，英文 `telemetry` 搜索正常；深层遥测 API 页可切换语言。
- Playwright 手机验证：390 × 844 尺寸下，从英文切换到对应中文 API 页成功，中文导航抽屉正常展开。
- 截图位于本地 `output/playwright/`，不提交到仓库。
- 已知非阻断情况：Material 的语言检测额外请求 `/zh/sitemap.xml`，而 i18n 插件生成根路径统一站点地图，该额外请求返回 404；上述语言切换及搜索实测正常。
- 当前为本地完成状态，尚未提交或推送 GitHub，线上站点未更新。

## 版本记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v0.1 | 2026-09-16 | 首次现状调研，原文件保留 |
| v0.2 | 2026-09-16 | 中英双语改版范围及验收标准 |
